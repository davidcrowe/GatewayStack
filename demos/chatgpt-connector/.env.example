PORT=5052

# Where to forward from the connector → gateway
GATEWAY_URL=http://localhost:8080
GATEWAY_READ_PATH=/protected/ping
GATEWAY_WRITE_PATH=/protected/echo

# (Optional) If your connector needs to know the issuer to emit PRM on 401:
OAUTH_PROTECTED_RESOURCE_METADATA=http://localhost:5051/.well-known/oauth-protected-resource
